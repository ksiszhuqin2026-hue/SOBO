# SOBO

材料配方单目标贝叶斯优化 Web 应用。

当前版本按新的 MatOpt 配方优化 UI 开发，覆盖 PRD 的核心闭环：

- 历史实验数据录入、CSV 上传、手动新增和数据校验
- 优化设置摘要与变量/约束编辑
- 简单采集策略：前期用 UCB，中期用 EI，后期用 PI
- 变量名称、上下限和变量数量可编辑
- 基于历史最佳值生成下一轮推荐配方
- 推荐配方应用为待实验
- 实验结果回填、更新模型并生成下一轮
- 实验数据 CSV 导出

## 运行

直接用浏览器打开 `index.html` 即可，无需安装依赖。
