# 材料配方单目标贝叶斯优化 Web 应用 PRD

## Summary
面向聚合物/树脂研发工程师，建设一个 MVP Web 应用，用于管理配方优化项目、导入历史实验数据、基于单目标贝叶斯优化推荐下一轮候选配方，并支持人工实验后回填结果形成迭代闭环。

核心闭环为：创建优化项目 -> 定义组分比例变量与目标方向 -> 导入/录入实验数据 -> 系统推荐 1-20 个候选配方 -> 研发工程师线下实验 -> 回填目标值 -> 继续优化。

## Key Changes / Product Requirements
- 项目管理：支持创建聚合物/树脂配方优化项目，配置项目名称、目标指标、优化方向（最大化或最小化）、实验备注和状态。
- 配方变量：首版以组分比例为主，支持每个组分的名称、单位、上下限、是否固定、比例总和约束；默认总和为 100%。
- 数据入口：支持 CSV/Excel 导入历史实验数据；支持手动新增、编辑、批量补录实验记录。
- 数据校验：导入和录入时校验必填字段、比例上下限、比例总和、目标值类型、重复配方，并给出可修正的错误提示。
- 贝叶斯优化：基于已有实验数据训练单目标代理模型，推荐用户设定数量的候选配方，候选数量范围为 1-20。
- 推荐解释：每个候选配方展示预测目标值、不确定性、推荐理由、与历史最佳配方的差异和预期改进。
- 实验闭环：用户可将推荐候选标记为“待实验、已实验、跳过”，并在实验完成后回填真实目标值与备注。
- 可视化：展示历史最佳值趋势、实验数据表、推荐候选表、目标值分布，以及当前最佳配方。
- 导出：支持导出推荐候选和完整实验数据为 CSV/Excel。
- 权限：MVP 默认单用户/本地团队使用，不做复杂多角色权限；保留后续扩展空间。

## Interfaces / Data Shape
- 项目字段：`project_id`、`name`、`material_type=polymer_resin`、`objective_name`、`objective_direction=max|min`、`created_at`、`status`。
- 组分字段：`component_id`、`name`、`lower_bound`、`upper_bound`、`is_fixed`、`fixed_value`、`unit=%`。
- 实验记录字段：`experiment_id`、各组分比例、`objective_value`、`source=import|manual|recommended`、`status`、`notes`、`created_at`。
- 推荐结果字段：`recommendation_id`、各组分比例、`predicted_value`、`uncertainty`、`expected_improvement`、`reason`、`status`。
- 导入模板：第一行为列名，组分列与目标值列必填；系统提供可下载模板。

## Test Plan
- 创建项目：验证最大化/最小化目标均可配置并正确影响最佳值判断。
- 变量约束：验证组分比例上下限、固定组分、总和 100% 校验。
- 数据导入：验证合法 Excel/CSV 可导入；缺列、非数字、越界、总和错误、重复配方能被拦截并展示错误。
- 推荐生成：验证在满足最少实验数据后可生成 1-20 个候选；候选不重复且满足所有比例约束。
- 闭环回填：验证推荐候选可转为实验记录，回填真实目标值后重新参与下一轮优化。
- 可视化与导出：验证趋势图、最佳配方、候选表和导出文件内容一致。
- 边界场景：实验数据不足、目标值缺失、所有组分固定、可行空间为空时给出明确提示。

## Assumptions
- 首版不接入实验设备、LIMS 或 ELN，实验执行和结果回填由研发工程师人工完成。
- 首版材料场景聚焦聚合物/树脂，但变量模型保持通用，后续可扩展到其他材料体系。
- 优化变量首版以连续组分比例为主，不处理复杂离散原料选择和禁配规则。
- 模型解释采用基础解释，不做高级响应曲面、采集函数曲线或复杂模型诊断。
- MVP 优先保证可用闭环和数据质量，不优先建设企业级权限、审计、审批和运维监控。
